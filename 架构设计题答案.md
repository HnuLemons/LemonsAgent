
# 架构设计题答案
## 模块一：Context / Performance
一个 session 连续聊了 200 轮，context 快爆了。你会怎么做压缩？如何确保压缩后的对话仍然流畅？
答：
核心解决方案：分层记忆 + 摘要压缩 + 重要信息保留
### 1️⃣滑动窗口保留近期上下文
例如[1-150轮]对话进行压缩，[150-200轮]保留原文
### 2️⃣对旧对话的压缩
利用LLM进行压缩，将历史对话拆分成三部分进行存储：情景记忆、长期记忆、用户画像
同时提取关键事实进入长期记忆（Memory.md），针对性信息存入用户画像（比如用户喜欢简洁回答）
> 你是一个记忆整理员。你的任务是把一段被压缩的历史对话浓缩成三种产物，
> 同时保证已有的长期记忆和用户档案不被覆盖或丢失。
> <old_conversation> {old_conversation} </old_conversation>
> <current_memory> {current_memory} </current_memory>
> <current_user> {current_user} </current_user>
> <today_episode_so_far> {today_episode} </today_episode_so_far>
> 请严格产出以下三段 XML，缺一不可：
> 
> <updated_memory>
> MEMORY.md 的完整新版本。严格要求：
> 保留 <current_memory> 中所有仍然有效的条目；不要删除你不理解的内容
> 从 <old_conversation> 中提炼"当前核心目标 / 未完成任务 / 关键事实"合并进来
> 合并重复、归并同类
> 只在明显过时的情况下才删除条目
> 整份文本不超过 3000 字
> </updated_memory>
> <updated_user>
> USER.md 的完整新版本。严格要求：
> 仅当 <old_conversation> 中有明确的用户偏好信号（如"我喜欢/不喜欢/以后都这样做"）时才修改对应章节
> 否则原样返回 <current_user> 的内容
> </updated_user>
### 3️⃣基于Token动态触发压缩
不是固定200轮就压缩，而是根据Token数量，当前token使用量超过最大量的80%时进行压缩
### 4️⃣重要性排序
做记忆的重要性排序，筛选重要系数高的记忆作保留，删去重要系数低的记忆
# 模块二：Memory
和聊天 Agent 熟悉半个月后，用户问了一个以前问过的问题。Agent 如何做 memory 召回更合理？
答：
1️⃣首先记忆会划分为三个层级：会话记忆、情景记忆、语义记忆。检索时需要分层检索
2️⃣在召回方式上采用多路召回与排序，根据用户问题，按需采取向量语义检索、关键词匹配等方法，检索到的内容需要做排序，排序模型的关键特征有语义相似度，记忆新鲜度，重要性评分等
3️⃣当检索到的内容与用户提问具有高度重复性，Agent需要做进一步的意图判断，可将意图分为三类：
- 确认型：比如“上次我说的餐厅怎么样”，直接召回当时对话内容，并以辅助记忆姿态给出答案
- 状态更新型：比如“那个项目进度如何”，检索当时结论后需要触发一个时效性检查
- 重复型：比如“LangChain的架构是怎么样的”，可以重新整理信息直接给出回答即可。
4️⃣记忆需要自我更新，既要做事实更新，也要给重复提及的内容做结构化标注。
# 模块三：Task 
对于长程任务，大模型执行一段时间可能会忘掉目标，你知道哪些解决方案，有什么优缺？
答：
核心思路：让 Agent 不仅依赖上下文窗口记忆，而是引入显式的任务规划、状态管理和长期记忆机制
## 1️⃣任务规划
对于一个确定性任务，Agent需要先拆解任务
如开发学生管理系统->拆解为下述任务
- 需求分析
- 技术文档
- 数据库设计
- 接口设计
- 后端开发
- 前端设计
- 测试部署
## 2️⃣外部状态管理
不指望Agent自己存储状态而是将状态保存到外部存储，设计状态数据结构
将Agent目前完成的任务（需求分析、技术文档、数据库设计）标记已完成，正在完成的任务（接口设计）标记为完成中，其他任务标记为未完成。每次调用模型时都将这些内容用Json格式注入提示词，让Agent明确当前任务状态
## 3️⃣思维链总结➕ReAct架构
将Agent已完成任务的成果做压缩，重点放在正在做的任务，每完成一个任务就做一次成果压缩，避免已完成任务占用过多上下文窗口。
引入ReAct结构，，通过周期性反思不断确定目标，确定方向
## 4️⃣长期记忆机制
将任务规划写进长期记忆，进行阶段性反思时时刻确定目前任务方向是否在任务规划内，同时如果在任务过程中需要修改任务规划也要同步更新长期记忆。
# 模块四：Tool / Session Runtime
Agent 工具有同步和异步两类。异步工具不能让用户一直等，但结果依然重要。你会如何设计异步工具执行和完成通知？
答：
## 异步工具调用流程
### 1️⃣Agent提交异步任务
立即返回任务ID，可继续其他任务
### 2️⃣异步任务进消息队列
进入消息队列如Kafka后由Worker节点完成任务
### 3️⃣任务状态管理
需要设计taskState，用于保存任务id，任务状态等信息，同时可以设计任务超时时间防止任务死循环导致队列阻塞。
## 消息通知机制
当任务完成时，需要输出 session_id、reslut等字段
需要使用消息中间件如Kafka、RabitMQ，解耦为执行层和通知层，完成的任务事件进入消息队列等待发送。由于保存了session_id，可以快速拼接回原Session，保证上下文完整
如果用户在线则直接发送，若用户不在线依旧保存taskState等待用户上线后发送
对于聊天Agent，多使用WebSocket进行实时双向通信
# 模块五：Agent Runtime 架构对比
Claude Code 的工具输出方式和国内 GLM / 豆包等 OpenAI-compatible function calling 有什么不同？他们各自这样设计的优缺点是什么？
答：
核心区别：Claude Code将Tool Calling 设计成 Agent 原生交互能力；OpenAI-compatible Function Calling 将 Tool Calling 设计成 API 层的函数调用能力。
## 核心区别
### 1️⃣工具调用模式
ClaudeCode的工具调用由Agent自主决定自主执行而OpenAI-compatible是严格遵守API调用标准。
ClaudeCode把工具调用作为模型推理循环的一部分，按照Tool Use → Tool Result → Continue Reasoning 的流程自主完成复杂任务。但是OpenAI-compatible只是模型生成结构化函数参数，具体实现还是在业务层
### 2️⃣Agent能力
ClaudeCode的工具调用更接近原生Agent的Tool Use， ClaudeCode Agent具有更强的自主任务规划能力和管理能力，OpenAI-compatible则需要额外的应用层去实现任务规划等复杂任务。
### 3️⃣工具调用的可控性
由于OpenAI-compatible工具调用是业务层执行，具有更高的可控性和安全性。相比之下ClaudeCode的安全性不如OpenAI-compatible。
### 4️⃣兼容性和标准化
OpenAI-compatible可以兼容国内大多数模型，只要采用openAI API，同一套代码即可切换多套模型。
由于业务层掌握参数校验、权限检查、审批流程等任务，OpenAI-compatible具有更强的工程控制能力，同一套流程，换不同的模型执行依然可以保证任务准确性。